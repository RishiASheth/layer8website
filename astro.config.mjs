// Comment the below code while running locally
/* 
import { defineConfig } from 'astro/config';

export default defineConfig({
     site: 'https://jois-code.github.io',
     base: '/layer8website',
});
*/